# Todos os arquivos de texto: LF no repositório
* text=auto eol=lf

# Scripts shell: sempre LF (falham no Linux com CRLF)
*.sh text eol=lf

# Dockerfiles: sempre LF
Dockerfile* text eol=lf

# Windows-específico: pode ficar com CRLF
*.ps1 text eol=crlf
